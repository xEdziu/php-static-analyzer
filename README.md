# PHP Static Analysis Tool

This is repository that contains the solution to recruitment task for an internship position at **JetBrains**.

The task was to write a program that simulates a light-weight static analysis tool for PHP code. The goal here was to implement `TypeResolver` that determines the type of PHP variable based only on its `@var` documentation block using the API provided with the task.


